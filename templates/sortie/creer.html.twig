{% extends 'base.html.twig' %}

{% block body %}


    <h2>Créer une sortie</h2>
    <div>
        {{ form_start(sortieForm) }}
        <div>
            {{ form_row(sortieForm.nom) }}
            <div class="row">
                <div class="col-4">
                    {{ form_label(sortieForm.dateHeureDebut) }}
                </div>
                <div class="col-4">
                    {{ form_widget(sortieForm.dateHeureDebut) }}
                </div>
            </div>

            {{ form_row(sortieForm.duree) }}
            {{ form_row(sortieForm.dateLimiteInscription) }}
            {{ form_row(sortieForm.nbMaxInscription) }}
            {{ form_row(sortieForm.infoSortie) }}

        </div>
        <div>
            <label class="col-4">Campus : </label>
            <label class="col-4">{{ app.user.campus.nom }}</label>
            {{ form_row(sortieForm.ville) }}
            <div class="row">
                <div class="col-8">
            {{ form_row(sortieForm.lieu) }}
                </div>
            <button type="button" value="Creer un lieu" class="btn btn-lg btn-secondary col-2">
                <a  class="text-white" href="{{ path('lieu_creer') }}">Creer un lieu</a>
            </button>
            </div>

         {#  <label>Rue</label>
            {{ lieu.rue }}
            <br>
            <label>Code postal</label>
            {{ lieu.ville.codePostal}}
            <br>
            <label>Latitude</label>
            {{ lieu.latitude }}
            <br>
            <label>Longitude</label>
            {{ lieu.longitude }} #}

        </div>

        <div class="mt-5 text-center">
            <input type="submit" class="btn btn-lg btn-secondary" value="Enregistrer" name="enregistrer">
            <input type="submit" class="btn btn-lg btn-secondary" value="Publier" name="publier" >
            <input  type="reset" class="btn btn-lg btn-secondary" value="Annuler" onclick= "document.location.href='{{ path('sortie_listeSortie') }}'">
        </div>

        {{ form_end(sortieForm) }}
    </div>
{% endblock %}

{% block title %}

{% endblock %}
{# }lieu select option foreach sur variable lieux envoyer dujavascript dans il faut une action javacript pour crée une variable lieu
    rue p sur la variable lieu.rue #}